#include "monty.h"

/**
 * main- Entry function
 * @argc: argument number.
 * @argv: argv.
 *
 * Return: return 0 if success and 1 if doesn't
 */

int main(int argc, char *argv[])
{
	handle_file_instrctions(argc, argv);
	return (0);
}
